name: jikan_moe
description: Dart/Flutter client for Jikan API (MyAnimeList).
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  http: ^1.5.0
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  queue: ^3.4.0
  test: ^1.25.6
